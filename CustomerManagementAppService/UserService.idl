import "AuthUserResultVM.idl";

namespace CustomerManagementAppService
{
    [bindable]
    [default_interface]
    runtimeclass UserService 
    {
        UserService();
        AuthUserResultVM GetUserByID(Int32 Id);
        AuthUserResultVM AuthenticateUser(String username, String password, Int64 companyId);
        AuthUserResultVM CreateUser(String username, String password, Int64 companyId);
    }
}
